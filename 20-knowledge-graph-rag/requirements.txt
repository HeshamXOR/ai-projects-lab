# Core math/graph/extraction are implemented FROM SCRATCH (no networkx, no spaCy).
# numpy is available for any vector work; the graph and extractor are pure Python.
numpy>=1.24
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0
pytest>=8.0
httpx>=0.27
